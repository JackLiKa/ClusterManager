# 集群管理平台项目文档

## 1. 本次需求补充说明

基于当前实现，需求补充明确为：

- 控制台在开发运行时，直接访问 `http://localhost:8080/` 必须可用。
- 集群看板中的服务节点间距必须明显，避免多个服务挤在一起影响辨识。
- 不论是伪集群还是集群管理模式，都必须支持手工添加和删除服务。
- 伪集群不仅要支持 TAP 虚拟节点，还要支持把本机已启动的 RocketMQ NameServer、Broker 作为宿主节点接入。
- 宿主节点必须能够参与真实 RocketMQ 的消息收发，不只是拓扑展示。
- 新增的服务必须进入拓扑、运维、日志、监控和 RocketMQ 消息模拟链路。

## 2. 实现摘要

### 2.1 启动可用性

为避免 `spring-boot:run` 时只有后端、没有前端静态资源的问题，构建链路已调整为：

- `generate-resources` 阶段执行前端构建
- 同阶段把 `frontend/dist` 复制到 `target/classes/static`

因此：

- 开发模式下访问 `http://localhost:8080/` 可直接打开页面
- 打包后运行 jar 仍然保持原有访问方式

### 2.2 统一服务注册能力

后端统一服务注册模型：

- `ServiceRegistration`
- `IServiceRegistry`
- `ServiceRegistrationCommand`
- `ServiceRegistrationRequest`

并将其并入 `IClusterProvider`，使所有 provider 都统一支持：

- `registerService(...)`
- `deleteService(...)`

### 2.3 伪集群实现

伪集群现在拆成两类节点：

1. `HOST`
   - 用于接入本机或局域网内已经启动的 RocketMQ NameServer / Broker / Proxy
   - 地址录入真实 `IP:Port`
   - 不会启动本地伪进程
   - 会进入拓扑、运维、日志、监控和消息模拟链路

2. `VIRTUAL`
   - 用于创建 TAP 虚拟服务节点
   - 地址录入虚拟 IP
   - 会进入虚拟网络段并由本地伪节点运行时承载
   - 可用于拓扑演示、本地链路模拟和混合联调

伪集群额外支持：

- 自动校验虚拟 IP 是否冲突
- 支持删除手工注册的伪节点
- 为新增和删除动作记录审计日志

限制与行为：

- 默认种子节点不可删除
- 只要伪集群中存在至少一个 `HOST` 类型的 NameServer，消息模拟就优先走真实 RocketMQ
- 这时宿主 Broker 与虚拟 Broker 会共享同一个消息验证入口

### 2.4 宿主节点真实消息桥接

为满足“宿主节点真正参与 RocketMQ 消息收发”的要求，新增 `PseudoRocketMqBridge`：

- 基于真实 RocketMQ Java Client 直连 NameServer
- 在伪集群消息模拟阶段发消息到真实 Topic
- 再从真实队列拉取消息，作为“发送成功 / 可消费”的确认

当前判定规则：

- 生产者或消费者中只要包含 `HOST` 节点，即启用真实 RocketMQ 桥接
- NameServer 地址从伪集群中注册的 `HOST` 类型 NameServer 节点解析
- 若没有可用的宿主 NameServer，则消息模拟直接返回失败结果并提示原因

### 2.5 真实集群实现

真实集群当前仍采用“真实 provider + 手工服务表”的组合方式：

- 原有 RocketMQ Admin 数据继续保留
- 手工添加的服务会并入拓扑节点集合
- 手工服务会生成到 NameServer 的拓扑连线
- 手工服务支持 Start / Stop / Restart / Delete
- RocketMQ 消息模拟默认也会覆盖这些手工节点

## 3. 前端交互调整

### 3.1 导航与布局

首页调整为：

- 顶部导航栏：模式切换、刷新、教程入口
- 首屏摘要卡片：模式、Provider、Active VIP、节点概览
- 主体双栏：左侧拓扑与消息工作台，右侧运维与服务登记

目标是在 100% 缩放比例下更充分显示关键信息。

### 3.2 运维面板

`OperationsPanel.vue` 已扩展为：

- 节点启停
- 手工服务添加
- 手工服务删除
- 伪集群下切换 `Host Node / Virtual Node`
- 刷新后自动回显到拓扑与表格

### 3.3 拓扑图

`ClusterTopologyCard.vue` 已调整：

- 支持拖动画布
- 支持拖动单个节点
- 支持滚轮缩放
- 支持悬停查看节点详情
- 宿主节点与虚拟节点采用不同形态展示

## 4. API 说明

服务管理接口：

```http
POST /api/clusters/{mode}/{middleware}/{clusterId}/services
DELETE /api/clusters/{mode}/{middleware}/{clusterId}/services/{nodeId}
```

伪集群下可通过 `labels.nodeKind` 指定节点类型：

```json
{
  "nodeId": "host-rmq-ns-01",
  "displayName": "Host NameServer 01",
  "role": "nameserver",
  "hostName": "localhost",
  "address": "127.0.0.1",
  "port": 9876,
  "labels": {
    "source": "console",
    "nodeKind": "HOST"
  }
}
```

虚拟节点示例：

```json
{
  "nodeId": "virt-broker-01",
  "displayName": "Virtual Broker 01",
  "role": "broker-master",
  "hostName": "virt-broker-01.local",
  "address": "10.77.0.40",
  "port": 19931,
  "labels": {
    "source": "console",
    "nodeKind": "VIRTUAL"
  }
}
```

## 5. 验证建议

建议按下面顺序验证：

1. 执行 `mvn -s .mvn/local-settings.xml -gs .mvn/local-settings.xml spring-boot:run`
2. 浏览器打开 `http://localhost:8080/`
3. 切到伪集群模式，先注册一个 `HOST` 类型 NameServer，例如 `127.0.0.1:9876`
4. 再注册一个 `HOST` 类型 Broker，例如 `127.0.0.1:10911`
5. 继续注册一个 `VIRTUAL` 类型 Broker，例如 `10.77.0.40`
6. 确认拓扑图、运维表、日志流都出现这些节点
7. 在消息工作台选择 Broker 发起验证，确认宿主 RocketMQ 参与消息收发

## 6. 后续建议

当前功能已经覆盖“宿主节点接入 + 虚拟节点联调”的主链路，但若要进一步生产化，建议继续推进：

1. 为手工服务登记增加持久化存储，避免应用重启后丢失。
2. 为宿主 RocketMQ 桥接补充更精细的消费确认与超时重试。
3. 为拓扑图加入按角色分层布局和保存拖拽位置能力。
4. 为宿主节点补充更真实的运行指标采集，而不是仅以占位指标展示。
