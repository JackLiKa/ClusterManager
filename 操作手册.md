# 集群管理平台操作手册

## 1. 当前版本重点

本次版本重点补齐了四件事：

1. 修复开发模式下 `http://localhost:8080/` 的稳定访问。
2. 首页重排为“顶部导航 + 首屏摘要 + 主体双栏”。
3. 伪集群支持同时接入宿主 RocketMQ 节点与 TAP 虚拟节点。
4. 宿主节点可以真正参与真实 RocketMQ 的消息收发验证。

## 2. 访问方式

开发模式：

```powershell
mvn -s .mvn/local-settings.xml -gs .mvn/local-settings.xml spring-boot:run
```

访问：

- `http://localhost:8080/`
- `http://localhost:8080/api/clusters/providers`

前端热更新模式：

```powershell
cd frontend
npm run dev
```

访问：

- `http://localhost:5173`

说明：

- 前端开发模式下 `/api`、`/ws`、`/guide` 仍代理到 `http://localhost:8080`
- 因此前端本地调试时，教程页也能直接打开

## 3. 顶部导航

顶部导航栏现在包含：

- 模式切换
- 刷新
- 教程入口

操作顺序建议：

1. 先切换到本地伪集群或真实集群
2. 再录入节点
3. 再检查拓扑、消息工作台和运维面板

## 4. 伪集群模式

### 4.1 两类节点

伪集群模式下，服务登记面板支持两类节点：

#### `Host Node`

用于接入你电脑上已经启动的 RocketMQ 节点，例如：

- NameServer：`127.0.0.1:9876`
- Broker：`127.0.0.1:10911`

特点：

- 使用真实地址和端口
- 不启动本地伪进程
- 会进入拓扑图、运维表、日志流
- 可参与真实 RocketMQ 的消息收发验证

#### `Virtual Node`

用于创建 TAP 虚拟节点，例如：

- `10.77.0.40`
- `10.77.0.41`

特点：

- 使用虚拟 IP
- 会加入伪集群虚拟网络
- 会由本地伪节点运行时承载
- 可与宿主节点一起出现在同一套拓扑中

### 4.2 推荐接入顺序

如果你要让宿主 RocketMQ 参与伪集群消息流，建议按下面顺序操作：

1. 先添加一个 `Host Node` 类型的 NameServer
2. 再添加一个或多个 `Host Node` 类型的 Broker
3. 如需模拟补充节点，再添加 `Virtual Node`
4. 最后到消息工作台选择 Broker 发起验证

### 4.3 消息工作台规则

伪集群下的消息工作台现在遵循以下规则：

- 如果生产者或消费者中包含 `Host Node`
  - 系统优先通过真实 RocketMQ NameServer 发消息并拉消息确认
- 如果全部都是 `Virtual Node`
  - 系统继续走本地伪节点运行时完成模拟

前提条件：

- 伪集群内至少要有一个 `Host Node` 类型的 NameServer
- 否则真实 RocketMQ 消息桥接无法启动

## 5. 真实集群模式

真实集群模式仍支持：

- 录入真实 NameServer / Broker / Proxy
- 手工删除已录入服务
- 在控制台中查看拓扑、日志、消息模拟结果

说明：

- 真实集群的拓扑和管理能力当前仍有一部分来自 `MockRocketMqAdminClient`
- 但伪集群中的宿主节点消息桥接已经可以真正连接到 RocketMQ 做消息验证

## 6. 拓扑图交互

拓扑图现在支持：

- 鼠标滚轮缩放
- 拖动画布
- 拖动单个节点
- 鼠标悬停查看节点详情

节点显示规则：

- `Host Node` 用独立形态展示
- `Virtual Node` 维持原有伪集群风格
- NameServer、Broker Master、Broker Slave、Proxy 按角色分列排布

## 7. 服务登记示例

### 7.1 宿主 NameServer

```json
{
  "nodeId": "host-rmq-ns-01",
  "displayName": "Host NameServer 01",
  "role": "nameserver",
  "hostName": "localhost",
  "address": "127.0.0.1",
  "port": 9876,
  "labels": {
    "source": "console",
    "nodeKind": "HOST"
  }
}
```

### 7.2 宿主 Broker

```json
{
  "nodeId": "host-rmq-broker-01",
  "displayName": "Host Broker 01",
  "role": "broker-master",
  "hostName": "localhost",
  "address": "127.0.0.1",
  "port": 10911,
  "labels": {
    "source": "console",
    "nodeKind": "HOST"
  }
}
```

### 7.3 虚拟 Broker

```json
{
  "nodeId": "virt-rmq-broker-01",
  "displayName": "Virtual Broker 01",
  "role": "broker-master",
  "hostName": "virt-rmq-broker-01.local",
  "address": "10.77.0.40",
  "port": 19931,
  "labels": {
    "source": "console",
    "nodeKind": "VIRTUAL"
  }
}
```

## 8. 验证建议

建议按下面顺序验证：

1. 启动你本机的 RocketMQ NameServer 和 Broker
2. 执行 `mvn -s .mvn/local-settings.xml -gs .mvn/local-settings.xml spring-boot:run`
3. 打开 `http://localhost:8080/`
4. 切到 `Local Pseudo`
5. 添加一个 `Host Node` 类型 NameServer
6. 添加一个 `Host Node` 类型 Broker
7. 如有需要再添加一个 `Virtual Node`
8. 在拓扑图确认节点都出现，并能拖动、缩放、悬停查看详情
9. 到消息工作台选择 Broker 发起验证
10. 在日志面板确认新增、删除和消息验证结果

## 9. 当前注意事项

- 没有 `Host Node` 类型 NameServer 时，宿主 RocketMQ 消息桥接不会工作
- `Virtual Node` 的地址仍必须是合法且不冲突的虚拟 IP
- 默认种子节点仍不可删除
- 当前宿主节点的监控指标还是占位值，不是来自 RocketMQ 的真实 runtime 指标
